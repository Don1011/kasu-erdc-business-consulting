import Layout from '../layout/Layout';

const NotFound = () => {
    return (
        <Layout 
            jumboShow = {false}
            pageTitle = "404, Page not found!"
        />
    )
}

export default NotFound
